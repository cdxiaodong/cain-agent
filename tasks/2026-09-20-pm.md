# 2026-09-20 下午单（Lead）

> 上午单任务 1（A3）已闭环；任务 2（组合回归）未领，并入本单。5-A 已收官，本单开启 5-B 首片。
> 模式：监工领取制；红线/署名/三门/done 规范同前单。

## 1. test：5-A 组合回归（上午任务 2 原样）

分支 `test/2026-09-20-phase5a-integration`。新增 `tests/test_phase5a_integration.py`（不改 src/，缺陷仅记录）：
- gate blocked → dry-run → 空 findings → 报告 caveats 无重放清单（链式）
- replay 证据 + 验证池分歧 → dissent 与重放清单同现不污染
- gate 缺失 + 旧数据无 replay 键 → 全链兼容
- ≥8 例

## 2. feat：5-B1 patterns 蒸馏格式 + 首批（Phase 5-B 开启）

分支 `feat/2026-09-20-patternlib`。按 ROADMAP B1：
- `skills/patterns.jsonl`：五字段假设格式（target_kind/invariant/violation/verify_method/confidence_prior），从既有 web 技能蒸馏 **≥20 条**（内容取自 skills/web/ 既有方法论，不新造靶场）
- `src/cain_agent/patternlib.py` 校验器：加载时五字段完整性/类型/confidence_prior∈(0,1] 校验，坏行拒绝带行号
- tests ≥8 例（格式合法/缺字段/类型错/越界置信/空文件/20 条全过）
