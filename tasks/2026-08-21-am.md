# 2026-08-21 派活单（监工补位）

> Lead 连续 7 次未履职，本单由监工依据 08-19 smoke 发现的问题下发。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支开发，禁直提 main；
> 写文件用 shell heredoc；代码注释/文档不点名任何参考项目。

## Claude 主力 — `feat/2026-08-21-scopeguard-fix`（高优先）

smoke 实测发现 ScopeGuardHook 命令语义误拦：grep 正则、本地路径、`2>` 重定向
被误判为 target,48 次 deny 全属此类，agent 被迫绕行。
任务：修 `ScopeGuardHook` 的命令解析——只有真实网络请求的目标 host 才参与 scope
判定，grep 模式/本地文件路径/重定向目标不得误拦。补回归测试钉死这些误拦场景
(素材见 `docs/release/smoke-2026-08-19.md` 问题 2 与 workspace 日志)。

## Codex-A — `feat/2026-08-21-cloud-deps`

云端测试套件在无 boto3 等依赖的环境收集即 11 个 ModuleNotFoundError(main 预存
在，CI 会挂）。
任务：pyproject 声明 cloud extras 或把云模块改懒加载，保证 `pip install -e .`
裸环境 `pytest` 收集零错误；补一个 CI 式的裸环境收集测试。

## Codex-B — `feat/2026-08-21-finding-fixture`

smoke 因靶场 nginx 405 合法地产出 0 findings,confidence/依据链缺真实数据闭环。
任务：在 bench 里造一个**本地可复现**的漏洞场景 fixture（如本地起的带 SSTI 的
Flask 小应用或离线 mock)，让编排链路能稳定产出 ≥1 finding 并完成
confidence+依据链全链路演练，写进 `docs/release/`。

---

交付标准：测试全绿 + 署名正确 + 各自分支提交并 push origin,完成后 mailbox 汇报。
