# 2026-08-28 派活单（监工 · Phase 2.7 第二波）

> pi 生态四项已闭环(冒烟/provider/工具面/双后端 bench,991 绿)。本波推进
> 剩余五项中的三项,模型路由押后。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支(建议独立 worktree),
> 禁直提 main;heredoc 写文件;注释/文档不点名任何参考项目。

## Claude 主力 — `feat/2026-08-28-pi-orchestration`

中心编排 + FindingsPipeline 双会话(发现≠校验)在 pi 后端实测。
任务:对自建靶场跑 `--backend pi` 编排模式全链路,验证 Manager 聚合/验证池/
语义记忆在 pi 后端行为等价;校验会话零工具语义实测(桥不注册工具+Python 侧
二次白名单校验双保险);发现与校验 provider 可不同配置的组合冒烟。
产出 `docs/release/pi-orchestration-2026-08-28.md`。

## Codex-A — `feat/2026-08-28-pi-docs-ci`

README(中英)双后端文档 + CI 收口。
任务:① 中英 README 增补 `--backend pi` 用法段(安装/环境变量/网关,对齐
`toolchain/pi/README.md`);② `toolchain/pi/package-lock.json` 锁版本入库;
③ CI workflow:Node 缓存 + 桥依赖安装步骤,保证 pi 协议测试在 CI 稳定。

## Codex-B — `feat/2026-08-28-pi-skills`

技能加载适配:SkillLoader 三阶段技能在 pi 后端的 prompt 验证。
任务:对 recon/test/report 各挑一个代表性技能(SQLi/文件上传/报告),在 pi
后端验证技能 prompt 注入格式无截断、无 token 膨胀异常;发现不适配处就地修
(优先改 Python 侧拼装,桥不动);记录 pi 后端下的技能加载行为差异。
产出 `docs/release/pi-skills-2026-08-28.md`。

---

模型路由(pipeline 级混搭)下一波再排。交付标准:测试全绿 + 署名正确 +
push origin + mailbox 汇报。
