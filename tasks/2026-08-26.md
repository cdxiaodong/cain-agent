# 2026-08-26 派活单（Lead · 晚间补位）

> 派活：2026-08-26 20:25（09:30/14:00 cron 因 Mac 睡眠积压触发）
> 执行：夜间或明日 09:30 唤醒后开工
> 背景：v0.2.0 已就绪（版本号/发布说明/978 绿，凌晨已合入）；cloud-deps 收口，CI 阻塞清零。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支（建议独立 worktree），
> 禁直提 main；heredoc 写文件；注释/文档不点名参考项目。

## 价值线

① 双后端等价性验证（Phase 2.7 benchmark 维度）② pi 国产模型实测（Phase 2.7 providers）③ pi 工具面（Phase 2.7 tools）

## Claude 主力 — `feat/2026-08-26-bench-backends`

任务：bench 框架加 **backend 对比维度**——同一场景（本地 SSTI fixture，稳定产出 finding）
分别以 `--backend claude` 与 `--backend pi`（网关 glm）跑编排链路，输出
确认率/耗时/token 对比表（对齐 bench-orchestrated-2026-08-19 格式）。
注意：若网关配额受限，至少完成 pi 侧一轮完整跑分 + claude 侧复用既有数据，
未跑项明确标"未测量"。产出 `docs/release/bench-backends-2026-08-26.md`。

## Codex-A — `feat/2026-08-25-pi-providers`（08-25 原任务顺延，今日必交付）

任务：真实跑通至少两家非 anthropic provider（**deepseek 强烈优先**——国产模型是
双后端核心价值）。key 走环境变量不入库；记录各家模型行为差异（工具调用稳定性/
JSON 输出纪律/中文指令遵循）。产出 `docs/release/pi-providers-2026-08-26.md` 对比表。

## Codex-B — `feat/2026-08-25-pi-tools`（08-25 原任务顺延，今日必交付）

任务：桥内工具面扩展——注册 Read/Grep/Glob 工具，工具调用同样走 tool_request
回 Python 判决（只读类给放行 matcher；**禁止把放行逻辑写进桥**）。
`allowed_tools` 白名单语义对齐 claude 后端（`["Bash","Read","Grep","Glob"]`
按 CLI 传值生效）。补 `tests/test_pi_executor.py` 工具面用例。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`

## Lead 备注

- v0.2.0 正式 tag/Release 待用户拍板后打（checklist 已就位，docs/release/v0.2.0-checklist.md）
- 08-26 日更已由凌晨验收收口保住（664f05f/c1324d2/a2d5912）
