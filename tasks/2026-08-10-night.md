# 产线派活单 · 2026-08-10 夜班（Day 10 · 第三波）

> **派活时间**: 2026-08-10 02:30（夜间自动派活）
> **验收时间**: 2026-08-10 08:00（早验收）
> **Lead**: 本会话（Codex CLI · 监工）

---

## 背景说明

Day 10 已完成的交付（Lead 直接提交，非团队产出）：
- AWS S3 暴露检测模块 ✅ 已推送
- 腾讯云 COS 暴露检测模块 ✅ 已推送
- 华为云 OBS 暴露检测模块 ✅ 已推送
- CSRF 检测技能 ✅ 已推送
- XXE 检测技能 ✅ 已推送
- 传播文章更新 ✅ 已推送
- ROADMAP + CHANGELOG 更新 ✅ 已推送
- main 上 520 测试全绿 ✅

**本次夜班任务为 ROADMAP 剩余推进项，请各位独立完成。**

---

## 任务分配

### Claude 主力工程师（faa9204d）— IAM/RAM 提权路径图可视化

**分支**: `feat/2026-08-10-iam-graph`

**任务**: 为阿里云 RAM 提权分析模块添加路径图可视化能力

**交付物**:
1. `src/cain_agent/cloud/iam_graph.py` — 提权路径图生成器
   - 从 `RamPrivescAnalyzer` 的分析结果生成 DOT 格式有向图
   - 节点：用户/角色/策略（不同颜色区分）
   - 边：提权路径（标注攻击动作：AttachPolicyToSelf / CreateAccessKey 等）
   - 输出 `to_dot()` 方法返回 Graphviz DOT 字符串
2. `tests/test_iam_graph.py` — 全 mock 单测
   - 测试空结果不报错
   - 测试单用户单路径生成正确 DOT
   - 测试多用户多路径去重
   - 测试 DOT 语法正确性（节点/边格式）
3. `docs/iam-graph-demo.md` — 可视化演示文档（含 DOT 示例 + 渲染说明）

**技术要点**:
- 参考现有 `aliyun_ram.py` 的 Finding 结构
- DOT 格式不依赖外部库（纯字符串生成）
- 零触网零真实凭证

**验收标准**:
- ruff/pyright/pytest 全绿
- commit 署名：`cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`
- 完成后提交 `done-claude.md` 汇报

---

### Codex-A（15470647）— AWS S3 vulnerable-terraform 靶场场景

**分支**: `test/2026-08-10-vuln-tf-aws`

**任务**: 新增 AWS S3 公开桶靶场场景，扩展 benchmark 覆盖

**交付物**:
1. `vuln-tf/aws-s3/main.tf` — Terraform 靶场
   - 一个 `aws_s3_bucket` 公开桶（acl = "public-read"）
   - 一个对照私有桶（acl = "private"）
   - Purpose 标签标注用途
   - 头五行授权声明
   - **不创建真实 AccessKey 资源**
2. `vuln-tf/aws-s3/variables.tf` — region + bucket_prefix 变量
3. `vuln-tf/aws-s3/outputs.tf` — 检测输出（bucket name / acl / expected_finding）
4. `vuln-tf/aws-s3/README.md` — 三步使用说明
5. `tests/test_vuln_tf_aws.py` — 静态校验测试
   - TF 文件存在性
   - 公开桶 ACL = public-read
   - 对照桶 ACL = private
   - Purpose 标签存在
   - 无硬编码 AccessKey
   - terraform fmt 检查（可选 skip）

**技术要点**:
- 参考现有 `vuln-tf/aliyun-oss/` 目录结构
- 零真实凭证，零 terraform apply
- 纯静态测试钉死靶标语义

**验收标准**:
- pytest 全绿
- commit 署名正确
- 完成后提交 `done-codex-a.md` 汇报

---

### Codex-B（c9a2f3d4）— 第二篇技术文章 + Gitee 镜像准备

**分支**: `feat/2026-08-10-article-2`

**任务**: 撰写第二篇技术文章（校验闭环深度解析）+ Gitee 镜像配置

**交付物**:
1. `docs/articles/02-validation-loop-deep-dive.md` — 第二篇传播文章
   - 主题：「让 AI 自己查自己：渗透 Agent 的校验闭环设计」
   - 内容要点：
     - 痛点：AI 幻觉漏洞进入交付物的责任风险
     - 方案：双 Agent 独立会话校验 + 四状态结构化输出
     - 实现：FindingValidator 执行层 + 去重指纹 + 规则表收口
     - 演示：一条假阳性漏洞如何被校验流水线拦截
   - 字数：1800-3500
   - 含 3 个标题候选
   - 含"授权"法律声明关键词
   - 无敏感字样（平安/pingan）
   - 无外露真实 IP（除 127.0.0.1）/域名（除 example.com）
2. `tests/test_article2_format.py` — 文章格式校验
   - 文件存在性
   - 字数范围
   - 标题候选数量
   - 授权关键词
   - 敏感信息红线检查
3. `docs/gitee-mirror-guide.md` — Gitee 镜像配置指南
   - 远程仓库添加步骤
   - 镜像推送命令
   - CI 配置同步说明

**验收标准**:
- 文章字数 1800-3500
- 格式校验全过
- commit 署名正确
- 完成后提交 `done-codex-b.md` 汇报

---

## 红线提醒

1. **禁用**：pentest-agent-mvp 代码（行业机密）、泄漏版 Claude Code 二开（法律风险）
2. **commit 署名**：必须使用 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`
3. **安全检测**：只读原则，不自动利用/验证性攻击
4. **共享工作树**：注意 git checkout 冲突，各自操作独立分支

---

## 汇报格式

完成任务后在仓库根目录提交 `done-{角色}.md`，包含：
- 分支名
- 改动文件清单
- 自测结果（ruff/pyright/pytest）
- 风险与说明

---

**派活**: Lead（本会话）
**验收**: 2026-08-10 08:00（早验收）
**下一次派活**: 验收后视完成情况决定
