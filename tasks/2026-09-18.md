# 2026-09-18 派活单(Phase 5-A 第 1 单:验证纪律强化)

> 派活:2026-09-17(用户批准 Phase 5 分期计划后起草;ROADMAP 总纲已落盘)
> 今天三任务推进 ROADMAP Phase 5-A:退出覆盖率硬门、可重放证据包、
> 验证分歧呈现。全部离线纯函数为主,零触网零新依赖。

## 基线与约束

- 基线 main=`6d74103`(1180 passed / 3 skipped,ruff/pyright 全绿)。
- 执行模式:监工领取制(领取回执→先钉死后改→实测验收→合并)。
- 通用要求:独立分支、禁直提 main、交付后工作树切回 main;commit 署名
  cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>(提交前验证);
  消息格式 feat/fix/test(scope): 描述;三门全绿(全量零回归);done 汇报
  写 tasks/done-2026-09-18-*.md。
- 红线:禁 pentest-agent-mvp / 泄漏 Claude Code / 平安内部信息;
  新增文本禁止出现任何外部报告来源标识(经验条目一律匿名化表述)。
- 缺省行为零变化原则:新门控/新字段全部带兼容开关或可选语义,
  不传参时行为与现状完全一致(对齐 Phase 2.7 模型路由先例)。

## 任务 1 — `feat/2026-09-18-coverage-gate`(A1 覆盖率退出门)

- 新增 `src/cain_agent/gates.py`:纯函数
  `check_coverage(endpoints: list[dict], probes: Mapping[str, int], config: CoverageConfig) -> CoverageVerdict`
  ——端点数下限/每端点最小探测数/比率阈值,返回 pass|blocked+原因列表
  (不依赖模型自评,退出校验由代码裁决)
- recon handler 接线(带 `--coverage-gate off` 开关,缺省开):blocked 时
  caveats 标记 `coverage_insufficient`,state.json 写 `recon_gate` 字段
- test handler 读 `recon_gate=blocked` → 降级 dry-run(不启动 test Agent,
  产物写说明文件);gate off 或 pass 时行为不变
- ≥10 例:空端点/低于阈值/恰好达标/开关关闭/probes 缺失/recon-test 接线

## 任务 2 — `feat/2026-09-18-evidence-replay`(A2 可重放证据包)

- `findings.py` 增可选 `replay` 字段:`{method, url, param_names: list[str],
  header_names: list[str]}`——**只收名称白名单键,不收任何值**(对齐
  web/bac_evidence 的敏感字段拒绝语义);from_dict/to_dict 往返,
  非法键(含 headers 值/cookie 值)显式拒绝
- `report_markdown.py` 新增「重放清单」节:仅列 method/url/参数名表,
  证据原文照旧只哈希(canary 断言:重放节不含任何值)
- ≥12 例:字段往返/非法键拒绝/canary 不泄漏/报告渲染三态/旧 findings
  (无 replay 字段)兼容

## 任务 3 — `feat/2026-09-18-model-dissent`(A4 分歧呈现)

- findings 增可选 `model_dissent` 字段:验证池反对票会话的理由摘要
  (list[{voter, reason}]),只含依据不含凭证(redact 后入库)
- `multi_agent/orchestration.py` 验证池表决处收集少数派理由(现丢弃)
  填入该字段;多数派一致时字段为空(兼容)
- `report_markdown.py` 详情节:有分歧时呈现「⚠ 分歧意见」子块
- ≥6 例:少数派收集/一致时为空/脱敏断言/报告渲染/兼容旧数据

(A3 confirm 副作用门顺延下一单:涉及 validator 第五状态与聚合联动,
独立成单降低耦合风险。)

---

交付标准:三门全绿 + 署名正确 + push origin + done 汇报 + 工作树切回 main。
push 备用(SSH 被拦时):`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
