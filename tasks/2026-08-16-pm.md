# 产线派活单 · 2026-08-16（下午场）

> **派活**: 2026-08-16 13:45（监工补位 — Lead 连续未派活）
> **验收**: 2026-08-16 17:45
> **背景**: 昨日三线（scope 端口闭环 / 并行验证池 / auto-prompt）已验收合入 main。本日推进中心编排器骨架接线 + 真实 smoke 复测。

---

## 任务分配

### Claude 主力工程师 — 并行验证池接入 FindingsPipeline

**分支**: `feat/2026-08-16-verify-pipeline`

**任务**: 把昨日 `verify_pool.py` 接入 `src/cain_agent/pipeline.py` 的校验环节——finding 校验改走并行验证池多数表决，替代单会话校验
- 保持「发现≠校验」双会话约束不破
- 降级：池不可用时回退单会话校验，不炸

**交付物**: `pipeline.py` 接线 + `tests/test_pipeline.py` 补充并行校验用例

**验收标准**: pytest 全绿；commit 署名 cdxiaodong

---

### Codex-A — 真实 smoke 复测（scope 修复后）

**分支**: `test/2026-08-16-smoke-retest`

**任务**: 对自建靶场 `http://103.236.66.228:3333/` 重跑 `cain-agent run`（非 dry-run），验证 scope 端口修复后 test 阶段能对 `POST /api/wishes` 上传面发探测
- 记录 findings 数量、被拦截情况、token 消耗
- 产出 smoke 报告 `docs/release/smoke-2026-08-16.md`

**交付物**: smoke 报告 + 必要时补 scope/工具层修复

**验收标准**: test 阶段不再被 scope 误拦；报告写实（未测标未测）；commit 署名 cdxiaodong

---

### Codex-B — 中心编排器语义记忆底座

**分支**: `feat/2026-08-16-semantic-memory`

**任务**: 在 `multi_agent/` 下实现「语义记忆」底座——finding/上下文向量化存储 + 相似度检索，供 solver 间共享可检索上下文（避免重复扫描）
- 纯逻辑零触网，可单测；检索接口与 Blackboard 对齐
- 不引入重依赖（向量可用本地 embedding 或接口抽象）

**交付物**: `multi_agent/memory.py` + `tests/test_memory.py`

**验收标准**: pytest 全绿；写文件用 shell heredoc；commit 署名 cdxiaodong

---

## 红线

1. 禁用 pentest-agent-mvp / 泄漏 Claude Code / 平安信息
2. commit 署名 cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>
3. 各自分支工作，禁止直接提交 main；写文件用 shell heredoc
4. 代码注释/文档不点名参考来源，只写设计本身
