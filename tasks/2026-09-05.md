# 2026-09-05 派活单（Lead · 深夜补位）

> 派活：2026-09-05 01:50（09-04 两窗口均未醒——失职×2 已由监工记录，本单跨 09-05 有效）
> 背景：监工已完成 v0.2.1 预案终备+发版守卫测试（1076 绿）。v0.2.1 发版**等用户拍板**
> （守卫测试锁定：待办不勾/预案不删/版本不 bump——任何人不得绕过）。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；独立分支，禁直提 main。

## 价值线

① 业务逻辑漏洞检测主线（DESIGN 差异化第一支柱，09-03 遗留）② 社区互动（回应式，不违反守卫）③ 产线自检文档

## 任务 1 — `feat/2026-09-05-bac-detector`（09-03 任务 3 重派，功能主线）

BAC/IDOR 检测器 v0（吸收 bacscan-blackbox 方法论）：
- `src/cain_agent/web/bac_detector.py`：双账号请求对重放（用户 A 正常请求 →
  用户 B 凭 A 的会话/ID 重放）+ 响应差异判定（状态码/资源归属字段/内容相似度
  阈值）→ BACFinding（对齐 findings.py 模型）；MBAC（未授权写）路径单独标记
- 纯逻辑 + 全 mock 测试（零触网零真实凭证），HTTP 执行层留注入接口
- 方法论对照：skills/ 既有 BAC 技能文档 + bacscan-blackbox 的判定标准

## 任务 2 — `chore/2026-09-05-community-respond`（社区回应，守卫口径内）

- issue #4-#8 / PR #3 **保持 open**（用户守卫指令）——本任务只做**回应**：
  在每条 issue 下留修复状态评论（修复已合入 4096e00，引用对应变更），
  PR #3 留 review comment（typo 确认+合并计划说明）
- 纯 gh CLI 评论操作，零关闭零合并，操作记录进 done 汇报

## 任务 3 — `docs/2026-09-05-pipeline-health`（产线自检）

`docs/pipeline-status.md`：产线健康画像一页纸——通道现状（工程师会话停摆 7 日/
Lead 会话不稳定/监工为当前主执行线）、近 7 日交付节奏表、风险清单（单点依赖
监工/cron 触发吞没）、建议（用户侧动作：重建工程师会话或给监工扩权）。
供用户一眼看懂产线为什么慢了。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`

## Lead 备注（09-04 失职复盘）

09-03 17:18 后本会话连续两个派活窗口未醒（cron 触发被会话切换吞没的既有问题）。
缓解：任务单改为**提前一晚写好**（本单即此模式），晨间窗口只剩"确认送达"轻动作，
降低吞没损失。
