# 2026-09-14 派活单

今天三任务推进 DESIGN §3.1 报告一致性与 BAC 检测链（判定✓输入✓→数据准备+组合加固）：报告索引排序收尾、BAC 重放计划构造器、BAC 全链组合测试。全部离线纯函数，零触网零新依赖。

## 基线与约束

- 基线 main=`1a6685f`（1167 passed / 3 skipped，ruff/pyright 全绿）。
- 执行模式：监工领取制（Lead 跨会话派单→领取回执→先复现后改→实测验收→合并）。工程师会话仍停摆。
- 通用要求：独立分支、禁直提 main、交付后工作树切回 main；commit 署名 cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>（提交前验证）；消息格式 fix/feat/test(scope): 描述；三门全绿（ruff/pyright/pytest 全量零回归）；done 汇报写 tasks/done-2026-09-14-*.md。
- 红线不变：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安内部信息。

## 1. fix：报告证据索引排序一致性

分支 `fix/2026-09-14-evidence-index-sort`。范围：`src/cain_agent/report_markdown.py`、`tests/test_report_trust_boundary.py`（仅反转记录性断言）。

昨日任务 3 记录性发现：`_render_evidence_index` 无 sorted 按输入序渲染，与表格/详情的定级排序不一致。修复：索引渲染对齐既有排序语义（severity 定级序+置信度降序，与表格/详情一致）；既有记录性测试按 done 汇报预告反转断言。不改 `_escape_cell` 等其它函数。

## 2. feat：BAC 重放计划构造器（纯数据层）

分支 `feat/2026-09-14-bac-replay-plan`。范围：新增 `src/cain_agent/web/bac_replay.py`、`tests/test_bac_replay.py`。

`build_replay_plan(pair: RequestPair, substitution: SessionSubstitution) -> ReplayPlan` 纯函数：
- `SessionSubstitution`：目标会话标识（cookie/header 名占位，不含值——**不接收任何真实凭证**）
- `ReplayPlan`：重放请求描述符（method/url/需要替换的会话携带位置列表/对象归属校验点）
- 约束：只做数据构造不做 HTTP；同账号/身份缺失输入直接拒绝（对齐 09-09 任务 1 前置检查）；输出可序列化（to_dict）
- ≥8 例测试：合法构造/同账号拒绝/缺身份拒绝/替换位置枚举完整性/to_dict round-trip

## 3. test：BAC 全链组合测试（判定 × 输入）

分支 `test/2026-09-14-bac-chain-tests`。范围：新增 `tests/test_bac_chain.py`（不改 src/）。

组合端到端纯函数链：`load_evidence → judge_bac`，覆盖组合边界：
- 合法证据（跨账号+读越权特征）→ horizontal 判定贯通
- 合法证据 + 403 + 依赖页变化（任务 1 修复③的组合场景）→ MBAC 优先且置信≤0.55
- 输入层拒绝（敏感键/类型冒充）→ 不产生任何判定（异常传播验证）
- 同账号在输入层合法但判定层排除（两层语义差异钉死）
- ≥10 例；失败用例只记录不修（按 09-09 任务 3 先例）

---

交付标准：三门全绿 + 署名正确 + push origin + done 汇报 + 工作树切回 main。push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
