# 产线派活 · 2026-08-08(Day 6)

> Lead:Claude · 当前阶段:**Phase 2 收尾(剩首轮 benchmark + 传播文章)+ 真跑能力补全**
> 依据:`/Users/cdxd/Desktop/develop/cloudpentest-agent/DESIGN.md` + ROADMAP.md + CHANGELOG.md
> 昨日(Day 5)状态:校验流水线、CredRedactHook、vuln-tf 场景二合入,冒烟测试首跑通过(安装/CLI/编排循环 exit 0),main 停在 `6db566d`,275 测试全绿。star 59 / fork 10。
> 命名说明:产线 Day 6,顺移命名 2026-08-08;分支名/done 文件统一跟随。

## 全局强制要求(三人通用,违反一律打回)

1. **分支隔离**:各自在指定新分支工作(从最新 main 切出),**禁止直接提交 main**,禁止改别人的文件范围。
2. **commit 署名**:所有 commit 必须是 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`。提交前执行 `git config user.email` 验证,不对先修正再提交。
3. **commit message 规范**:`feat(scope): 描述` / `test(scope): 描述` / `docs(scope): 描述`,禁止无意义 message。
4. **质量门**:提交前本地 `ruff check src tests`、`pyright`、`pytest` 三绿。
5. **完成后汇报**:在仓库根写 `done-XX.md`(分支名、改动文件清单、自测结果、验收对照),Codex 写文件一律用 shell heredoc,**别用 apply_patch**。
6. **红线**:不使用 pentest-agent-mvp 任何代码;不引入泄漏版 Claude Code 代码;Agent 引擎只用 Claude Agent SDK;不涉及任何平安内部信息;云操作一律只读。

## 今日冻结文件(已验收,只 import 不改)

`executor.py` / `scope.py` / `workspace.py` / `orchestrator.py` / `findings.py` / `validator.py` / `pipeline.py` / `redact.py` / `cloud/aliyun_oss.py` / `cloud/aliyun_ram.py`

---

## 任务 1 ·【Claude 主力工程师】真实 StageHandler — recon/test 阶段接入 SDKExecutor(架构级)

- **分支**:`core/2026-08-08-stage-handlers`
- **目标**:冒烟证明编排循环能跑,但跑的是 placeholder。今天把 recon/test 两个阶段的**真实 handler** 做出来:由 SDKExecutor 驱动 Agent 执行侦察与测试,prompt 里注入对应阶段的 skills 知识(阶段加载省 token, DESIGN §2),产物结构化落盘。这是"AI 渗透工程师"真正能干活的核心件。
- **改动范围**(只允许动这些):
  - 新建 `src/cain_agent/handlers.py`
  - 新建 `tests/test_handlers.py`
- **功能要求**:
  1. `make_recon_handler(executor, skill_loader) -> StageHandler`:prompt 组装 = 阶段目标(recon:存活验证/技术栈指纹/端点提取,**禁止资产扩张**,DESIGN §3.1)+ scope.yaml 约束复述 + recon 阶段技能内容;executor 返回后解析产物写 `workspace/recon/`(endpoints 草稿 + 原始文本)。
  2. `make_test_handler(executor, skill_loader) -> StageHandler`:读 recon 产物 + assets.json,对范围内端点跑 L1 探测(L2/L3 后续),注入 skills/web 三技能;发现的疑似漏洞按 `Finding` 模型落 findings.json(`result` 初值 `validation_inconclusive`,留给校验流水线)。
  3. `SkillLoader`:按阶段加载 `skills/` 下 SKILL.md(phase 字段匹配 recon/test),只注入当前阶段技能;技能文件缺失时降级为无技能 prompt 并记录,不炸。
  4. 所有 Agent 输出进 workspace 前过 `redact.redact` 脱敏(Day 5 合入的件第一次实战接线);finding 内容标 `[UNTRUSTED_DATA]` 边界在 validator 已有,handlers 只负责脱敏。
  5. handler 必须幂等友好:同阶段重跑覆盖同名产物文件,不追加膨胀。
- **自测**:fake executor(预置返回文本)+ tmp_path workspace + 真实 skills/web 文件,零 token 零触网。覆盖:recon 产物落盘结构、test handler 生成合法 Finding(过 findings.from_dict 校验)、SkillLoader 按阶段过滤、脱敏接线(输出含假 AK 串被脱敏)、重跑幂等。
- **不做**:不接真实目标跑端到端(留 benchmark)、不改 CLI(任务 2 并行)、不实现 L2/L3。

## 任务 2 ·【Codex-A】CLI run 子命令 — 把编排循环接进命令行(功能开发)

- **分支**:`feat/2026-08-08-cli-run`
- **目标**:现在 CLI 只有 `--version`。把 Orchestrator 接进来:`cain-agent run --target <host> --workspace <dir>` 一条命令走完 recon→test→report,这是演示视频和真实用户的第一触点。
- **改动范围**(只允许动这些):
  - 修改 `src/cain_agent/cli.py`
  - 新建 `tests/test_cli_run.py`
  - 新建 `templates/workspace.example/` 或在 README 用法段补充(二选一,别都搞)
- **功能要求**:
  1. argparse 子命令结构:`run`(主命令)+ 保留 `--version`。`run` 参数:`--target`(必填,写入 scope.yaml 的 in_scope)、`--workspace`(默认 `./workspace`)、`--idle-timeout` / `--total-budget`(透传 executor)、`--dry-run`(只初始化 workspace + scope 并打印执行计划,不起 Agent)。
  2. 安全接线(全部复用冻结件,不重新发明):Workspace 初始化 → scope.yaml 写入 target(仅它一个 in_scope)→ ScopeGuardHook 挂载 → executor 构造(allowed_tools 默认 `["Bash","Read","Write"]` 之外的都不给,首期先 `["Bash"]` 即可,注释说明) → placeholder handler 跑三阶段(真实 handler 是任务 1,CLI 只依赖现有 Orchestrator 公开接口,**接口若对不上就在汇报中记录,不许改 orchestrator.py**)。
  3. 危险防护:`--target` 若不是 `127.0.0.1`/`localhost`/`.local` 后缀/私网 IP,打印**二次确认提示**要求显式加 `--i-have-authorization` 才继续(授权声明留痕,DESIGN 法律边界);`--dry-run` 不需要。
  4. 退出码:正常 0;scope 初始化失败/参数非法 2;中断(预算/超时)0 但打印 partial 摘要。
  5. 全程不打印凭证环境变量值;stdout 摘要含各阶段产物路径。
- **自测**:进程内调 main(monkeypatch argv),覆盖:dry-run 初始化、无授权标志跑公网 target 被拒、127.0.0.1 直通、非法参数 exit 2。**严禁真实启动 Agent 循环烧 token**(executor 用 monkeypatch 替身)。
- **不做**:不接真实 handler(任务 1 完成后 Lead 统一接线)、不动 pyproject 依赖。

## 任务 3 ·【Codex-B】第一篇传播文章初稿 + 演示视频分镜脚本(文档)

- **分支**:`test/2026-08-08-article-demo`
- **目标**:ROADMAP Phase 2「第一篇传播文章」+ Phase 1 遗留「演示视频」的素材准备。star 已到 59,传播杠杆到位,文章发布前由用户亲审。
- **改动范围**(只允许动这些):
  - 新建 `docs/articles/2026-08-draft-1-ai-pen-aliyun.md`
  - 新建 `docs/demo-script.md`
  - 新建 `tests/test_article_format.py`
- **功能要求**:
  1. 文章初稿(2000-3000 字,中文,目标渠道 FreeBuf/先知/掘金):标题候选 3 个(主打"首个懂国产云的实战 AI 渗透 Agent");结构:痛点(CTF 玩具 vs 实战空位)→ 差异化(国产云+业务逻辑+校验闭环)→ 架构一图流(文字描述版,留图位标记 `[图:架构]` )→ 真实能力演示(OSS 暴露检测 + RAM 提权路径,用**合成示例数据**,严禁真实目标/真实漏洞细节)→ 工程哲学(确定性框住智能体)→  roadmap 与引流(GitHub 链接占位)。文末附"授权测试"法律声明段。
  2. **事实红线**:文内所有能力声明必须对应仓库已合入的真实功能(findings 校验闭环/ScopeGuard/CredRedact/OSS/RAM/Docker),不许吹牛;star/fork 数字不写死(用"发布时更新"占位);不涉及任何平安内部信息、不出现真实厂商漏洞细节。
  3. `docs/demo-script.md`:3 分钟演示视频分镜脚本——0:00-0:20 痛点引入、0:20-1:00 `pip install` + `cain-agent run --target 127.0.0.1 --dry-run`、1:00-2:00 编排循环跑动 + findings 校验闭环产物展示、2:00-2:40 云模块(OSS/RAM,用 vuln-tf 靶场画面)、2:40-3:00 roadmap + star 引导。每镜配旁白文案和画面说明。
  4. `tests/test_article_format.py`:静态校验——文章存在、字数在 1800-3500 区间、含法律声明关键词("授权")、无 `平安|pingan` 字样、3 个标题候选齐全、无外露 IP/域名(除 example.com 与 127.0.0.1)。
- **不做**:不发布任何内容到外部平台、不改 README、不配图(留标记)。

---

## 验收标准(21:00 Lead 验收对照表)

| 任务 | 合并前提 |
|---|---|
| 1 Handlers | 三绿;Finding 合法性/技能过滤/脱敏接线/幂等有测试钉死;冻结文件零改动 |
| 2 CLI run | 三绿;公网 target 无授权标志被拒有测试钉死;零真实 Agent 启动 |
| 3 文章+分镜 | 三绿;事实声明与已合入功能一一对应;无敏感信息 |

## Lead 备注

- 冒烟已过、CLI 即将可用、文章素材就位后,下周重点:首轮 benchmark(vuln-tf 靶场真实跑一次,需要用户在自有账号提供只读 RAM 子账号凭证,**等待用户决策**)与演示视频录制(需人工)。
- 遗留待用户拍板:ScopeGuardHook 本地只读命令白名单豁免(继续挂起)。
