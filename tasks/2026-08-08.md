# 产线派活单 · 2026-08-08（Day 7）

> **派活时间**: 2026-08-07 15:30（立即执行）
> **验收时间**: 2026-08-07 21:00
> **特殊说明**: Day 6 已提前验收推送，Day 7 立即开工，确保 GitHub 日更

---

## 今日交付目标

**推进 Phase 3 扩能**: 云模块扩腾讯云 + Web 技能补全

---

## 任务分配

### Claude 主力工程师（faa9204d）— 腾讯云 COS 暴露检测

**分支**: `feat/2026-08-08-tencent-cos-exposure`

**任务**: 实现腾讯云对象存储（COS）暴露检测模块，对齐阿里云 OSS 暴露检测的设计模式

**交付物**:
1. `src/cain_agent/cloud/tencent_cos.py` — CosExposureChecker 类
   - `__init__(access_key_id, access_key_secret, region)` — 支持从参数/环境变量读凭证
   - `check_public_buckets()` -> list[CosFinding] — 枚举存储桶并检测公开访问
   - `_is_bucket_public(bucket_name)` — 检查 ACL/策略是否公开
   - `_check_sensitive_files(bucket)` — 检测敏感文件模式（.env/.key/.pem 等）
2. `tests/test_tencent_cos.py` — 全 mock 单测（零触网，零真实凭证）
   - 测试缺凭证报错
   - 测试只读 API 调用（GetBucket / GetBucketAcl）
   - 测试公开桶检测逻辑
   - 测试敏感文件模式匹配

**技术要点**:
- 使用 `requests` + 腾讯云 API v3 签名（HMAC-SHA1 或 HMAC-SHA256），参考阿里云 RAM 的自实现模式
- 凭证环境变量: `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY`
- 只读 API: GetService / GetBucket / GetBucketAcl / GetBucketPolicy
- CosFinding 格式对齐 OssFinding（cloud=tencent, service=cos, resource=bucketName）

**验收标准**:
- ruff check / pyright / pytest 全绿
- commit 署名正确（cdxiaodong + noreply 邮箱）
- 完成后提交 `done-claude-core.md` 汇报

---

### Codex-A（15470647）— OWASP CSRF 技能

**分支**: `feat/2026-08-08-csrf-skill`

**任务**: 实现 CSRF（跨站请求伪造）检测技能，补充 OWASP Top10 能力

**交付物**:
1. `skills/web/csrf/SKILL.md` — CSRF 检测技能文档
   - 原理：GET/POST 状态改变操作缺少 CSRF Token / Referer 校验
   - 检测方法：抓包分析 → 提取状态改变请求 → 去除 Token / 伪造 Referer → 重放
   - 工具：curl / Burp Suite（命令行模式）
   - 输出格式：finding（csrf, 端点, payload, 验证步骤）
2. `tests/test_csrf_skill.py` — 技能文档格式校验（如 test_article_format.py 模式）

**技术要点**:
- 重点检测：登录后状态改变操作（修改用户信息/转账/删除资源）
- 判断依据：响应是否表明操作成功（状态码 200+ 特征关键字）
- 不依赖靶场，文档描述检测方法和 Payload 结构

**验收标准**:
- 技能文档完整（原理/检测/工具/输出）
- 格式校验通过（中文无错字/标题层级/代码块）
- commit 署名正确
- 完成后提交 `done-codex-a.md` 汇报

---

### Codex-B（31d15820）— OWASP 文件上传漏洞技能

**分支**: `feat/2026-08-08-file-upload-skill`

**任务**: 实现文件上传漏洞检测技能，补充 OWASP Top10 能力

**交付物**:
1. `skills/web/file-upload/SKILL.md` — 文件上传漏洞检测技能文档
   - 原理：未校验文件类型/内容/大小，允许上传 Webshell/恶意文件
   - 检测方法：上传测试文件（.phtml/.jsp/.asp）→ 访问验证是否执行
   - 绕过技巧：双写后缀/ MIME 伪造/图片马
   - 工具：curl / Burp Suite
   - 输出格式：finding（file-upload, 端点, payload, 路径）
2. `tests/test_file_upload_skill.py` — 技能文档格式校验

**技术要点**:
- 重点检测：头像上传/附件上传/文件导入功能
- 测试 Payload：无害验证文件（如 GIF89a<?php phpinfo();?>）
- 不依赖靶场，文档描述检测方法和 Payload 结构

**验收标准**:
- 技能文档完整（原理/检测/绕过/工具/输出）
- 格式校验通过
- commit 署名正确
- 完成后提交 `done-codex-b.md` 汇报

---

## 红线提醒

1. **禁用**：pentest-agent-mvp 代码（行业机密）、泄漏版 Claude Code 二开（法律风险）
2. **commit 署名**：必须使用 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`
3. **安全检测**：只读原则，不自动利用/验证性攻击
4. **Web 技能**：不依赖靶场，重点描述检测方法和 Payload 结构

---

## 设计文档推进

今日推进 **DESIGN.md Phase 3 第一条**：云模块扩腾讯云。完成后更新 ROADMAP.md 勾选状态。

---

**派活**: Claude Code（Lead）
**监工巡查**: 10:15 / 15:15 / 22:15
