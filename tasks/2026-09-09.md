# 2026-09-09 派活单

今天三任务分别推进 DESIGN §3.2 默认拒绝与数据边界、§3.3 证据驱动校验、§3.1 可审计报告：BAC 核心防误报修复、离线证据格式校验、报告敏感信息边界测试。均不新增靶场，不执行真实网络检测。

## 基线与约束

- 当前 main 基线 0fd3945；09-09 凌晨实测 ruff 全过、pyright 0 errors、pytest 1115 passed / 3 skipped。BAC 目前仅纯函数，不能宣称实战验证完成。
- 先审计既有 worktree/未提交修改。使用独立 worktree，禁止切换共享主工作树、禁止 reset/删除他人修改。提交前核验分支名、git config user.name、git config user.email。
- commit 作者必须为 cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>；消息 feat(scope): 描述 / test(scope): 描述 / fix(scope): 描述；署名尾注按当前会话要求。
- 三门：ruff check src tests；pyright -p pyproject.toml --pythonpath .venv/bin/python；pytest -q。不得跳过模块或降低门禁来宣称通过。
- 红线：不使用 pentest-agent-mvp 任何代码;不引入泄漏版 Claude Code 代码;Agent 引擎只用 Claude Agent SDK;不涉及任何平安内部信息。
- 不扩展或更换引擎；现有 pi 与上述引擎限制的历史差异不在本任务中裁决。版本/tag/Release、issue/PR 外发/关闭一律不做。

## 1. Claude 主力：BAC 身份与证据前置条件

分支 `core/2026-09-09-bac-evidence-guards`。
范围：src/cain_agent/web/bac_core.py、tests/test_bac_core.py。

已观察到 judge_bac 未检查 object_owner 与 replay_as 是否相同；owner_fields_match 为真且 B 响应成功就进入 horizontal。先复现同账号请求对误判，再修复：身份缺失/相同账号不得判为已成立水平越权；验证 similarity/threshold 的有限数值与范围，非法输入显式拒绝而非产生高置信结论。403 与 victim_side_change 同时出现属冲突证据，不能仅凭状态码声称防护在位；保留人工复核含义。兼容公开 API，避免单凭 2xx/相似度给 confirmed 标签。

至少覆盖同账号、空身份、NaN/Inf/越界阈值、拒绝响应但依赖页变化、正常跨账号对照。证据依据来自当前 bac_core 模块说明：资源归属先核验、公共资源排除、依赖页反馈优于直接响应；对照 DESIGN §3.3 四态及防自证。

## 2. Codex-A：离线 BAC 证据包输入验证

分支 `feat/2026-09-09-bac-evidence-input`。
范围：新增 src/cain_agent/web/bac_evidence.py、tests/test_bac_evidence.py。

新增纯函数 load_evidence(data: Mapping[str, object])，仅验证/构造当前 RequestPair 与 ResponseDiff，不调用 judge_bac，不与任务1共享文件。严格检查必要键、类型、HTTP 状态码范围、有限 similarity 0..1、VictimSideChange 合法值；未知字段拒绝，错误信息只含字段名，不包含凭证或输入内容。输入不得含 headers/cookies/token/body 明文；仅处理现有判定所需元数据。无文件 IO、HTTP、CLI 或新依赖。

覆盖合法构造、缺字段、错误类型（bool 冒充 int）、非法枚举/状态码、未知敏感字段以及错误消息不泄露假凭证。不把输入归一化结果当作漏洞已确认。

## 3. Codex-B：报告信任边界回归测试

分支 `test/2026-09-09-report-trust-boundary`。
范围：新增 tests/test_report_trust_boundary.py；如发现运行缺陷，仅记录定位与失败用例，不改另外两任务或报告实现。

针对 main 已合入 report_markdown.py 的纯渲染路径补边界测试：恶意表格分隔符/换行、HTML 字符、未知 severity、空结论、仅证据哈希输入、元数据损坏。不触网、不读取真实凭据，使用合成 canary 验证输出不意外包含不应落盘的输入字段；区分 HTML 转义与 Markdown 安全显示要求，先确认现有契约再断言。问题若可复现，done 报告写路径、行号、最小输入/预期/实际，不将失败测试标 completed。

## 汇报与验收

两位 Codex 写文件用 shell heredoc，不用 apply_patch。各自提交推送指定分支，写 tasks/done-2026-09-09-{claude,codex-a,codex-b}.md（对应本人），包含基线/commit/文件清单/三门原始摘要/阻塞。Lead 收结果后验收；入队不等于开工，未回执不标已送达完成。
