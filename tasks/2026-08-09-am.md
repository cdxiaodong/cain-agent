# 产线派活单 · 2026-08-09 上午（Day 8 · 第一波）

> **派活时间**: 2026-08-09 01:00（立即执行）
> **验收时间**: 2026-08-09 12:00（午验收）
> **节奏调整**: 每天两次派活（上午 01:00 / 下午 13:00），加速开发

---

## 今日交付目标

**继续推进 Phase 3 扩能**: 云模块扩 AWS + OWASP 技能补全 + 传播推进

---

## 任务分配

### Claude 主力工程师（faa9204d）— AWS S3 暴露检测

**分支**: `feat/2026-08-09-aws-s3-exposure`

**任务**: 实现 AWS S3 对象存储暴露检测模块，对齐阿里云 OSS / 腾讯云 COS 设计模式

**交付物**:
1. `src/cain_agent/cloud/aws_s3.py` — S3ExposureChecker 类
   - `__init__(access_key_id, secret_access_key, region)` — 支持参数/环境变量
   - `check_public_buckets()` -> list[S3Finding] — 枚举存储桶检测公开访问
   - `_is_bucket_public(bucket_name)` — 检查 ACL/Policy 是否公开
   - `_check_sensitive_files(bucket)` — 检测敏感文件模式
2. `tests/test_aws_s3.py` — 全 mock 单测（零触网零真实凭证）
   - 测试缺凭证报错
   - 测试只读 API 调用（ListBuckets / GetBucketAcl / GetBucketPolicy）
   - 测试公开桶检测逻辑

**技术要点**:
- 使用 `boto3`（AWS 官方 SDK，比手写签名更简洁）
- 凭证环境变量: `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`
- 只读 API: ListBuckets / GetBucketAcl / GetBucketPolicy / ListObjects
- S3Finding 格式对齐 OssFinding/CosFinding（cloud=aws, service=s3）

**验收标准**:
- ruff/pyright/pytest 全绿
- commit 署名正确（cdxiaodong + noreply 邮箱）
- 完成后提交 `done-claude-core.md` 汇报

---

### Codex-A（15470647）— OWASP XXE 技能

**分支**: `feat/2026-08-09-xxe-skill`

**任务**: 实现 XXE（XML 外部实体注入）检测技能

**交付物**:
1. `skills/web/xxe/SKILL.md` — XXE 检测技能文档
   - 原理：XML 解析器未禁用外部实体，可读取本地文件/SSRF
   - 检测方法：构造 XML Payload → 观察响应是否包含文件内容
   - Payload 示例：`<!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>`
   - 工具：curl / Burp Suite
   - 输出格式：finding（xxe, 端点, payload, 读取文件）
2. `tests/test_xxe_skill.py` — 技能文档格式校验

**技术要点**:
- 重点检测：XML 输入接口（SOAP / XML API / 文件导入）
- 测试 Payload：读取 `/etc/passwd`（Linux）或 `C:\Windows\win.ini`（Windows）
- 零触网零真实凭证

**验收标准**:
- 技能文档完整（原理/检测/工具/输出）
- 格式校验通过
- commit 署名正确
- 完成后提交 `done-codex-a.md` 汇报

---

### Codex-B（c9a2f3d4）— 传播文章二次优化

**分支**: `feat/2026-08-09-article-polish`

**任务**: 优化第一篇传播文章（docs/articles/2026-08-draft-1-ai-pen-aliyun.md），提升传播力

**交付物**:
1. 优化 `docs/articles/2026-08-draft-1-ai-pen-aliyun.md`
   - 标题优化（3 个候选 → 选 1 个最强钩子）
   - 开头痛点强化（前 100 字抓住读者）
   - 实战演示部分补充（突出国产云差异化）
   - 结尾 CTA 优化（Star/Fork/试用引导）
2. `docs/articles/publish-checklist.md` — 发布前检查清单
   - 标题钩子强度
   - 痛点共鸣度
   - 技术深度
   - 差异化突出
   - 法律声明完整
3. `tests/test_article_polish.py` — 优化后格式校验

**技术要点**:
- 参考爆款技术文章结构（痛点→方案→演示→CTA）
- 突出"首个懂国产云"差异化
- 保持 1800-3500 字
- 无敏感信息（平安/pingan/真实漏洞细节）

**验收标准**:
- 优化后文章字数符合要求
- 发布检查清单完整
- 格式校验通过
- commit 署名正确
- 完成后提交 `done-codex-b.md` 汇报

---

## 红线提醒

1. **禁用**：pentest-agent-mvp 代码（行业机密）、泄漏版 Claude Code 二开（法律风险）
2. **commit 署名**：必须使用 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`
3. **安全检测**：只读原则，不自动利用/验证性攻击
4. **AWS 模块**：使用 boto3 官方 SDK，零触网 mock 测试

---

## 设计文档推进

今日推进 **DESIGN.md Phase 3 第二条**：云模块扩 AWS + 传播文章优化。完成后更新 ROADMAP.md 勾选状态。

---

**派活**: Claude Code（Lead）
**验收**: 2026-08-09 12:00（午验收）
**下一次派活**: 2026-08-09 13:00（下午任务）
