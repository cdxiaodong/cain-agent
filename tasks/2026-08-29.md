# 2026-08-29 派活单（Lead）

> 派活：2026-08-28 17:00（14:00 窗口补执行）
> 执行：明日 09:30 唤醒后开工
> 背景：Phase 2.7 已 8/9 闭环（fcd2636），测试 1001 绿；本单收官 Phase 2.7 最后一项 + 启动 Phase 3。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支（独立 worktree），
> 禁直提 main；heredoc 写文件；注释/文档不点名参考项目。

## 价值线

① Phase 2.7 收官（模型路由深化）② Phase 3 启动（Gitee 同步）③ Phase 2 遗留闭环（vuln-tf 首轮跑分记录）

## Claude 主力 — `feat/2026-08-29-model-routing`（Phase 2.7 收官项）

任务：**pipeline 级模型混搭路由**——recon 阶段用低成本模型（pi+网关 glm）、
test 阶段用高能力模型（claude 或 pi+强模型），CLI 支持
`--recon-backend/--test-backend`（及对应 provider/model 参数），未指定时
保持现有全局 backend 行为零变化。补路由分发测试（两阶段各走各的 executor，
配置缺省回退）。产出说明进 README.zh-CN.md。

## Codex-A — `feat/2026-08-29-gitee-sync`（Phase 3 启动）

任务：Gitee 镜像同步——`scripts/gitee_sync.sh`（或 .py）：mirror push
main 到 Gitee 远端（凭证走环境变量 GITEE_TOKEN，脚本内零硬编码）；
README.zh-CN.md 加「国内镜像」一节说明同步机制；补 dry-run 模式测试
（不真推，验证远端配置与命令构造）。真推动作留用户手动首推一次。

## Codex-B — `test/2026-08-29-vuln-tf-bench`（Phase 2 遗留闭环）

任务：用已接入的 BenchmarkExecutor 对 vuln-tf 三场景（OSS 公开桶/RAM 过度
授权/RAM 管理员）跑一轮**离线静态跑分**并出报告：检出数/误报数（对照桶零
命中）/耗时/token，写入 `docs/release/bench-vulntf-2026-08-29.md`，
顺手把 ROADMAP Phase 2 的靶场+首轮 benchmark 条目数据补齐供勾选。
不 apply、不触网、零真实凭证。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
