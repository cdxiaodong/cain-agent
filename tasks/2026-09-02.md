# 2026-09-02 派活单（Lead · 下午补位）

> 派活：2026-09-02 15:15（09:30 窗口被会话切换中断，补执行）
> 背景：功能线全收官（1058 绿 + CI 绿牌）。本单清技术债 + 发布准备。
> 执行资源：监工/临时会话领取模式。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；独立分支，禁直提 main。

## 价值线

① 技术债清零（恢复 CI 硬门）② 发布物料完备 ③ 文档一致性

## 任务 1 — `fix/2026-09-02-pyright-zero`（22 个基线类型错误清零）

清掉 main 上 22 个 pyright 错误（清单：CI run 33468896484 日志——tencent_cam
返回类型/memory 返回路径/executor Optional/toolchain/workspace_ext/test_backend_bench/
test_ci_workflow 等），全部修完后把 ci.yml 的 pyright `continue-on-error: true`
移除恢复硬门。测试零回归。

## 任务 2 — `feat/2026-09-02-release-v020-final`（v0.2.0 发布物料终检）

对照 docs/release/v0.2.0-checklist.md 逐项核验：版本号一致（pyproject/__init__）、
CHANGELOG 中英摘要、发布说明文档、报告/README 三语言资产、CI 绿牌。缺项补齐，
输出核验记录进 checklist 文件。**不打 tag 不建 Release**（留用户拍板），
但准备一条 `git tag -a v0.2.0 -m ...` 的建议命令行写进 checklist。

## 任务 3 — `docs/2026-09-02-usage-guide`（用户指南）

`docs/usage.md` 快速上手：安装（pip install -e .[dev,cloud] / Node 桥 npm ci）→
首次运行（scope.yaml 配置示例 + cain-agent run --target ...）→ 双后端选择
（--backend pi + 网关配置）→ 报告解读（report.md 结构说明）→ FAQ（凭证环境
变量表：阿里/腾讯/华为/AWS/GCP/Azure + GITEE_TOKEN + PI_BASE_URL）。
中英双语或中文为主英文简要。格式测试。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
