# 产线派活单 · 2026-08-15（下午场）

> **派活**: 2026-08-15 13:46（监工补位 — Lead 自 08-12 起连续 3 天未派活未验收）
> **验收**: 2026-08-15 17:45
> **背景**: 08-12 CLI 真实 smoke 打通，但 test 阶段 0 findings——根因是 scope 白名单对带端口目标匹配未归一。本日修此 bug 并推进中心编排器。

---

## 任务分配

### Claude 主力工程师 — 修复 scope 端口匹配 bug

**分支**: `fix/2026-08-15-scope-port-match`

**任务**: Scope 白名单匹配时，把「带端口的请求目标」与「裸 host 的 in_scope 条目」归一后再比对
- 现状：`in_scope` 存 `103.236.66.228`，但 agent 请求 `103.236.66.228:3333` 被「默认拒绝」误伤
- 要求：`src/cain_agent/scope.py` 匹配入口对目标做 host 提取（剥 scheme/端口/路径），与 `cli.py` 的 `_scope_entry` 逻辑对齐
- 覆盖：IPv4:port / 域名:port / 带 scheme URL / IPv6 `[::1]:port`

**交付物**: `scope.py` 修复 + `tests/test_scope.py` 补充端口/URL 匹配用例

**验收标准**: pytest 全绿；对 `http://103.236.66.228:3333/` 重跑 smoke，test 阶段能对上传面发探测（不再被误拦）；commit 署名 cdxiaodong

---

### Codex-A — 中心编排器：并行验证池

**分支**: `feat/2026-08-15-parallel-verify`

**任务**: 在 `src/cain_agent/multi_agent/` 下实现「并行验证池」——同一 finding 并行起 N 个独立校验会话，交叉确认防单点误判
- 复用现有 `Blackboard` / `Solver` 骨架
- 输出：多数表决 + 分歧标记（`validation_consensus: confirmed|contested|rejected`）
- 纯逻辑零触网，可单测

**交付物**: `multi_agent/verify_pool.py` + `tests/test_verify_pool.py`

**验收标准**: pytest 全绿；写文件用 shell heredoc；commit 署名 cdxiaodong

---

### Codex-B — 中心编排器：失败自愈 auto-prompt

**分支**: `feat/2026-08-15-auto-prompt`

**任务**: 实现「失败模式自愈」——中心监控 solver 失败模式（连续空结果/超时/解析失败），自动重写提示词重派
- 输入：solver 历史失败记录（来自 Blackboard）
- 输出：重写后的 prompt + 重派决策（retry / decompose / skip）
- 纯逻辑零触网，可单测

**交付物**: `multi_agent/auto_prompt.py` + `tests/test_auto_prompt.py`

**验收标准**: pytest 全绿；写文件用 shell heredoc；commit 署名 cdxiaodong

---

## 红线

1. 禁用 pentest-agent-mvp / 泄漏 Claude Code / 平安信息
2. commit 署名 cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>
3. 各自分支工作，禁止直接提交 main；写文件用 shell heredoc
4. 代码注释/文档不点名参考来源（不提 XBOW/D-CIPHER/PentAGI/HPTSA 等名字），只写设计本身
