# 2026-08-25 派活单（监工补位 · pi 后端生态第一波）

> 双执行引擎已合入(main 86a507c):`--backend pi` 协议打通、19 例协议测试全绿。
> 本波把 pi 从「协议可跑」推进到「真实可用」。素材:`src/cain_agent/pi_executor.py`
> `toolchain/pi/`(先读 `toolchain/pi/README.md`)。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支(建议独立 worktree),
> 禁直提 main;heredoc 写文件;代码注释/文档不点名任何参考项目。

## Claude 主力 — `feat/2026-08-25-pi-smoke`(高优先)

桥侧 pi-agent-core API 按文档编写,**未跑过真实 LLM 调用**。
任务:① `cd toolchain/pi && npm install`(Node ≥ 20);② 最小真实调用打通,
API 字段(Agent 构造 / 事件 / usage)有出入就地修桥,Python 侧协议不动;
③ 对自建靶场 `http://103.236.66.228:3333/` 跑 `--backend pi` 非 dry-run 全链路;
④ scope 越权实测:故意构造 out-of-scope 命令,验证桥回 Python 判决且默认拒绝。
产出 `docs/release/pi-smoke-2026-08-25.md`(含 API 修正清单)。

## Codex-A — `feat/2026-08-25-pi-providers`(cloud-deps 未完则先收尾它)

任务:真实跑通至少两家非 anthropic provider(deepseek 强烈建议——国产模型
是双后端的核心价值;openai/google 次之)。key 走环境变量不入库;记录各家的
模型行为差异(工具调用稳定性 / JSON 输出纪律 / 中文指令遵循)。
产出 `docs/release/pi-providers-2026-08-25.md` 对比表。
若 08-24 的 cloud-deps 仍未交付,先完成它再开工本任务。

## Codex-B — `feat/2026-08-25-pi-tools`(v0.2.0 未发则先完成发版)

任务:桥内工具面扩展 —— 注册 Read / Grep / Glob 工具(参数与语义对齐现有
Bash 的拦截链路):Read/Grep/Glob 的工具调用同样走 tool_request 回 Python 判决
(Read/Grep/Glob 属只读,Python 侧给对应 matcher 的放行 hook 即可,禁止把
放行逻辑写进桥)。`allowed_tools` 白名单语义对齐 claude 后端
(`["Bash","Read","Grep","Glob"]` 按 CLI 传值生效)。
补 `tests/test_pi_executor.py` 工具面用例。

---

后续波次见 ROADMAP Phase 2.7(技能适配 / 编排实测 / benchmark 对比 / CI /
模型路由)。交付标准:测试全绿 + 署名正确 + push origin + mailbox 汇报。
