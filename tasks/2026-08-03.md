# 产线派活 · 2026-08-03(Day 1)

> Lead:Claude · 当前阶段:**Phase 0 收尾 → Phase 1 开工**
> 依据:`/Users/cdxd/Desktop/develop/cloudpentest-agent/DESIGN.md` + ROADMAP.md + CHANGELOG.md
> 昨日状态:Phase 0 骨架已落(pyproject / src/cain_agent / tests / CI / LICENSE / README / git 署名),main 停在 `7f6cd89`。

## 全局强制要求(三人通用,违反一律打回)

1. **分支隔离**:各自在指定新分支工作,**禁止直接提交 main**,禁止改别人的文件范围。
2. **commit 署名**:所有 commit 必须是 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`。
   提交前执行 `git config user.email` 验证,不等于上述邮箱先 `git config user.email "84082748+cdxiaodong@users.noreply.github.com"` 修正再提交(这决定 contributions 是否计入)。
3. **commit message 规范**:`feat(scope): 描述` / `test(scope): 描述` / `docs(scope): 描述`,禁止 "update" / "fix" 等无意义 message。
4. **质量门**:提交前本地跑通 `ruff check src tests`、`pyright`、`pytest`,不绿不准提交。
5. **完成后汇报**:分支名、改动文件清单、自测结果(ruff/pyright/pytest 输出结论)。
6. **红线**:不使用 pentest-agent-mvp 任何代码;不引入泄漏版 Claude Code 代码;Agent 引擎只用 Claude Agent SDK;不涉及任何平安内部信息;所有目标校验逻辑默认拒绝未授权目标。

---

## 任务 1 ·【Claude 主力工程师】SDKExecutor 最小封装(架构级)

- **分支**:`core/2026-08-03-sdk-executor`
- **目标**:Phase 1 第一个架构件。把 Claude Agent SDK 封装成项目统一的执行引擎入口,后续 Orchestrator / Hook / Skills 全部挂在它上面。
- **改动范围**(只允许动这些):
  - `pyproject.toml` — `dependencies` 加入 `claude-agent-sdk`
  - 新建 `src/cain_agent/executor.py`
  - 新建 `tests/test_executor.py`
- **功能要求**:
  1. `SDKExecutor` 类:构造参数含 `allowed_tools`(白名单,默认空列表=零工具)、`hooks`(PreToolUse hook 注册入口,预留 ScopeGuardHook 挂载点)、`idle_timeout`(默认 300s)、`total_budget` 上限参数、`resume_session_id`(session 断点续跑预留)。
  2. `async run(prompt: str) -> ExecutorResult`:内部走 SDK 的 `query()`,把消息流收敛为结构化结果(文本输出 / 工具调用记录 / 用量统计)。
  3. 超时与预算:idle 超时或总预算触顶时优雅中断并返回部分结果,不抛裸异常(设计文档 §3.2 死循环烧 token 教训)。
  4. 类型标注齐全,basic 模式 pyright 零错误。
- **自测**:`tests/test_executor.py` 用 mock/monkeypatch 替代真实 SDK 调用(不烧 token),覆盖:默认零工具白名单、hook 注册透传、idle 超时中断路径。
- **不做**:不接 Orchestrator、不实现任何具体 Hook(那是任务 2 的事)、不接真实云调用。

## 任务 2 ·【Codex-A】scope.yaml 加载 + ScopeGuardHook(安全先行)

- **分支**:`feat/2026-08-03-scope-guard-hook`
- **目标**:DESIGN.md「第一周行动清单」第 5 项。Scope 硬约束是整个平台的安全地基:目标在不在授权范围由工程强制校验,不靠 AI 自律。
- **改动范围**(只允许动这些):
  - 新建 `src/cain_agent/scope.py`
  - 新建 `templates/scope.example.yaml`
  - 新建 `tests/test_scope.py`
  - `pyproject.toml` — 仅允许追加 `pyyaml` 依赖(其他不动)
- **功能要求**:
  1. `Scope` 类:从 YAML 加载 `in_scope` / `out_of_scope` 列表,条目支持域名(含 `*.example.com` 通配)、裸 IP、CIDR 网段三种形态;加载即校验格式,非法条目直接报错。
  2. `Scope.is_allowed(target: str) -> bool`:先匹配 out_of_scope(拒绝优先),再匹配 in_scope;两者都不中默认**拒绝**(白名单原则)。
  3. `ScopeGuardHook`:符合 PreToolUse hook 签名的可调用对象,从工具入参中提取目标(host/url/ip),不在 scope 内则返回阻断决定。命令行字符串要能从 Bash 类工具的 command 参数里提取出 host(至少覆盖 curl/nmap/直接 URL 三种常见形态,提取不出来的一律阻断并提示)。
  4. `templates/scope.example.yaml`:带完整中文注释的示例,明确写「仅限自有或书面授权目标」。
- **自测**:`tests/test_scope.py` 覆盖:通配域名匹配、CIDR 包含/不包含、out_of_scope 优先、默认拒绝、Bash 命令目标提取(含提取失败阻断)。
- **不做**:不接 executor(Lead 负责集成)、不实现凭证脱敏 Hook。

## 任务 3 ·【Codex-B】骨架单测补齐 + 中文 README + 旧资产归档清单

- **分支**:`test/2026-08-03-tests-docs`
- **目标**:把 Phase 0 收尾的测试/文档债清掉,给 ROADMAP 上「GitHub topics 补全」「旧仓库归档整理」两项提供落地材料。
- **改动范围**(只允许动这些):
  - 新建 `tests/test_cli.py`
  - 新建 `README.zh-CN.md`
  - 新建 `docs/legacy-inventory.md`
  - 新建 `docs/github-repo-metadata.md`
- **功能要求**:
  1. `tests/test_cli.py`:用 `capsys` + monkeypatch `sys.argv` 测 CLI 的 `--version` 输出(含版本号)和无参数时打印帮助,进程内调用 `main()`,不起子进程。
  2. `README.zh-CN.md`:对照现有英文 README.md 逐段翻译,法律声明(仅限授权测试)必须放最前面,定位文案保持「实战型 AI 渗透工程师,非 CTF 靶场玩具」。
  3. `docs/legacy-inventory.md`:盘点仓库旧资产——`skills/`(65 个云技能)、`exploits/`、`tools/`、`templates/`、`docs/legacy/`,逐项列表标注【保留为知识库 / 待重构 / 建议归档】及一句理由,不删除任何文件。
  4. `docs/github-repo-metadata.md`:给出 GitHub 仓库 description、homepage、topics 建议清单(topics 从 DESIGN.md §5 推广节奏里挑:cloud-security / pentest / ai-agent / aliyun 等,凑满 10 个左右),供人工到 GitHub 后台填写。
- **自测**:`pytest tests/test_cli.py` 全绿;两个新文档 markdown 结构完整、无占位符。
- **不做**:不改英文 README.md、不动 skills/ 下任何文件内容、不碰 src/。

---

## 验收标准(21:00 Lead 验收对照表)

| 任务 | 合并前提 |
|---|---|
| 1 SDKExecutor | ruff/pyright/pytest 三绿;mock 单测覆盖超时路径;未接真实云调用 |
| 2 ScopeGuard | 默认拒绝语义有测试钉死;Bash 目标提取失败=阻断有测试钉死 |
| 3 测试/文档 | test_cli 全绿;两份文档无占位符;未改动范围外文件 |
