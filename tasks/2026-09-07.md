# 2026-09-07 派活单（Lead · 提前一晚模式）

> 派活：2026-09-07 00:42（深夜写就，白班窗口只需确认送达——治 cron 吞没）
> 背景：09-06 停摆一日（Lead 未醒）。main=1101 绿，v0.2.1 守卫锁定等用户拍板。
> 执行资源：监工/临时会话领取模式。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；独立分支，禁直提 main。

## 价值线

① 业务逻辑漏洞检测最小切片（大任务拆小提高被领取率）② 社区回应（守卫口径内）③ 日更恢复

## 任务 1 — `feat/2026-09-07-bac-core`（BAC 检测器最小切片，比 09-05 版缩小 60%）

只做**判定核心**（无 HTTP 层无 CLI）：
- `src/cain_agent/web/bac_core.py`：`RequestPair`（原始请求 A + 重放请求 B 元数据）、
  `ResponseDiff`（状态码/归属字段集/正文相似度三元组）、
  `judge_bac(pair, diff, config) -> BACVerdict`（horizontal/vertical/none +
  置信度 + 依据，纯函数零副作用）
- `tests/test_bac_core.py` ≥10 例：同资源归属一致=none / 200 vs 403=vertical 倾向 /
  200+他人数据=horizontal / 相似度灰区=低置信 / MBAC 写操作标记
- 方法论对照 bacscan-blackbox 判定标准（注释引用规则出处）
- **不做**：HTTP 执行、编排接入、CLI（后续任务）

## 任务 2 — `chore/2026-09-07-community-respond`（09-05 任务 2 重派）

issue #4-#8 / PR #3 **保持 open**（用户守卫）——只做回应评论：
修复状态 + 引用 commit 4096e00 对应变更；PR #3 留 review comment。
gh CLI 操作记录进 done 汇报。

## 任务 3 — `docs/2026-09-07-changelog-refresh`（CHANGELOG 补齐 09-05~07）

CHANGELOG.md 补 2026-09-05 条目（PR#9 采纳 + 健康画像 + vuln-tf CLI 修复 +
1101 绿）与 09-06 停摆备注；对齐 docs/release/ 既有日报。格式测试（若有
CHANGELOG 校验用例则更新）。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
