# 产线派活 · 2026-08-07(Day 5)

> Lead:Claude · 当前阶段:**Phase 2 校验闭环(3/5 已完成)**
> 依据:`/Users/cdxd/Desktop/develop/cloudpentest-agent/DESIGN.md` + ROADMAP.md + CHANGELOG.md
> 昨日(Day 4)状态:FindingValidator 执行层、RAM 提权分析、vuln-tf 场景一全部合入,main 停在 `6cb1da7`,222 测试全绿。star 21→48,fork 2→9。
> 命名说明:产线 Day 5,沿用顺移命名 2026-08-06+1=本文件 2026-08-07;分支名/done 文件统一跟随。

## 全局强制要求(三人通用,违反一律打回)

1. **分支隔离**:各自在指定新分支工作(从最新 main 切出),**禁止直接提交 main**,禁止改别人的文件范围。
2. **commit 署名**:所有 commit 必须是 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`。提交前执行 `git config user.email` 验证,不对先修正再提交。
3. **commit message 规范**:`feat(scope): 描述` / `test(scope): 描述` / `docs(scope): 描述`,禁止无意义 message。
4. **质量门**:提交前本地 `ruff check src tests`、`pyright`、`pytest` 三绿。
5. **完成后汇报**:在仓库根写 `done-XX.md`(分支名、改动文件清单、自测结果、验收对照),Codex 写文件一律用 shell heredoc,**别用 apply_patch**。
6. **红线**:不使用 pentest-agent-mvp 任何代码;不引入泄漏版 Claude Code 代码;Agent 引擎只用 Claude Agent SDK;不涉及任何平安内部信息;云操作一律只读。

## 今日冻结文件(已验收,只 import 不改)

`executor.py` / `scope.py` / `workspace.py` / `orchestrator.py` / `findings.py` / `validator.py` / `cloud/aliyun_oss.py` / `cloud/aliyun_ram.py`

---

## 任务 1 ·【Claude 主力工程师】Findings 校验流水线 — dedup + validator 串接成 report 前置阶段(架构级)

- **分支**:`core/2026-08-07-findings-pipeline`
- **目标**:把 Day 3 的数据模型(findings.py)和 Day 4 的校验执行层(validator.py)串成完整流水线,并经 Orchestrator 已有的 StageHandler 注入点挂上——Phase 2 头牌「校验闭环」至此端到端成型。**不改 orchestrator.py**,用注入实现组装。
- **改动范围**(只允许动这些):
  - 新建 `src/cain_agent/pipeline.py`
  - 新建 `tests/test_pipeline.py`
- **功能要求**:
  1. `FindingsPipeline` 类:输入 `Workspace` + 发现 executor + 校验 executor(独立 session);流程硬编码:读 workspace findings.json → `dedup` 去重 → 逐条 `FindingValidator.validate` → 写回 findings.json(经 Workspace 原子写)→ 产出校验汇总(各状态计数)。
  2. 幂等:已 `confirmed`/`false_positive` 的 finding 重跑不重复校验(跳过终态),只校验新发现或 `validation_inconclusive` 的;重跑结果一致(去重后数量不膨胀)。
  3. 容错:单条 finding 校验抛 `ValidatorConfigError` 以外的异常 → 该条标 `validation_system_error` 继续,不炸流水线;汇总里给出失败清单。
  4. `make_report_handler(pipeline)` 工厂:返回符合 `StageHandler` 协议的可调用对象,可直接 `Orchestrator(handlers={"report": ...})` 注入;handler 内先跑校验流水线再落占位报告产物(真实报告生成后续任务)。
  5. 汇总落盘 `workspace/report/validation-summary.json`:总数/各状态数/去重掉多少条/失败清单。
- **自测**:fake executor + tmp_path workspace,零 token 零触网。覆盖:全链路(新 finding → confirmed)、终态跳过幂等、单条异常容错、去重计数正确、handler 与 Orchestrator 真实组装跑通 recon→test→report 顺序。
- **不做**:不生成真实报告 markdown、不改任何冻结文件、不接真实云数据。

## 任务 2 ·【Codex-A】CredRedactHook — 凭证脱敏模块(第二道安全 Hook)

- **分支**:`feat/2026-08-07-cred-redact-hook`
- **目标**:DESIGN §3.2 安全工程第二件:所有文本(日志/工具输出/报告/findings)落盘或进上下文前,对 AK/SK、Token、私钥模式强制脱敏——发现中的"凭证样式字符串"只哈希不落明文。
- **改动范围**(只允许动这些):
  - 新建 `src/cain_agent/redact.py`
  - 新建 `tests/test_redact.py`
- **功能要求**:
  1. `redact(text: str) -> str`:模式表(代码常量,每个模式带名称)至少覆盖:阿里云 AK(`LTAI` 前缀)、AWS AK(`AKIA`/`ASIA` 前缀)、`sk-` 开头 API key、Bearer/JWT token(`eyJ` 前缀 JWT)、 PEM 私钥块、通用 `password= / secret: ` 键值对。替换为 `<REDACTED:类型:sha256前8位>`(可审计可追溯,不留明文)。
  2. 误报控制:短于 16 位的疑似命中不脱敏(防误伤普通文本);占位符样例(`AKIAIOSFODNN7EXAMPLE` 这类文档示例串)不脱敏,模式表里配示例白名单。
  3. `CredRedactHook`:符合 SDK PostToolUse hook 签名的可调用对象,对工具输出的文本内容跑 `redact` 后透传;结构非法的输入原样放行不炸。
  4. `redact_dict(data)` 辅助:递归处理 dict/list/str 结构(给 findings/报告 JSON 用)。
- **自测**:覆盖:每类模式命中脱敏、同一凭证两次出现哈希一致(可关联审计)、示例白名单不误伤、短串不误伤、嵌套 dict 递归、hook 结构非法放行。**测试中严禁出现真实凭证**,全部用构造的假串(如 `LTAI` + 16 个 `A`)。
- **不做**:不接 executor/orchestrator(Lead 后续统一挂)、不改动日志系统(当前还没有)。

## 任务 3 ·【Codex-B】vuln-tf 靶场场景二 — RAM 过度授权用户(给 RAM 提权模块做端到端靶标)

- **分支**:`test/2026-08-07-vuln-tf-ram`
- **目标**:靶场扩第二个场景:一个会被 Day 4 RAM 提权分析模块检出的误配置 RAM 用户,与场景一同构。
- **改动范围**(只允许动这些):
  - 修改 `bench/aliyun-vuln-tf/main.tf`(追加资源)、`variables.tf`、`outputs.tf`、`README.md`
  - 修改 `tests/test_vuln_tf.py`(追加场景二校验,不动场景一已有用例)
- **功能要求**:
  1. 场景二资源:一个 RAM 用户 `vuln-ram-user-<suffix>`,挂**自定义**过度授权策略(Action 含 `ram:AttachPolicyToUser` + `ram:CreateAccessKey`,Resource `*`)——正好命中 RAM 模块的 AttachPolicyToSelf/CreateAccessKey 两条规则;一个对照用户 `safe-ram-user-<suffix>` 只挂 `AliyunOSSReadOnlyAccess` 系统策略。
  2. 约束:不创建 AccessKey 资源(防止 apply 后真的泄出可用 AK)、vuln 用户不设登录密码(纯 API 实体);所有新资源打 `Purpose = "vuln-benchmark"` 标签;头部授权声明注释保持。
  3. `outputs.tf` 追加:两个用户名与预期检出(vuln 用户应命中 ≥2 条提权规则、对照用户应零命中)。
  4. `README.md` 追加场景二说明:用途(给 RAM 提权模块做端到端回归)、为什么不用真实 AK 资源(安全设计)、apply/destroy 警告复述。
  5. 测试追加:场景二资源存在、vuln 策略含两条提权 Action、对照用户只挂只读系统策略、无 `alicloud_ram_access_key` 资源类型(钉死"不建真 AK")、LTAI 扫描。**严禁** terraform init/apply。
- **不做**:不加场景三、不动 bench/docker-compose.yml、不动 src/ 和 docs/benchmark-plan.md。

---

## 验收标准(21:00 Lead 验收对照表)

| 任务 | 合并前提 |
|---|---|
| 1 Pipeline | 三绿;幂等/容错/与 Orchestrator 真实组装有测试钉死;冻结文件零改动 |
| 2 CredRedact | 三绿;模式命中+白名单+哈希一致性有测试钉死;测试中无真实凭证样式串 |
| 3 靶场场景二 | 三绿;"不建真 AK"有测试钉死;未执行任何 terraform apply |

## Lead 备注

- 增长数据:star 21→48 / fork 2→9(Day 4 验收时查),第一篇传播文章的杠杆价值正在变大——Day 6 计划派 Codex-B 起草文章初稿(用户审后发布)。
- 遗留待用户拍板:ScopeGuardHook 本地只读命令白名单豁免(继续挂起);演示视频待人工录屏。
