# 2026-08-24 派活单（监工补位）

> 08-23 全天 Mac 睡眠无巡查窗口、零 commit(日更由监工晚间补位),本单由监工下发。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支开发(建议独立 worktree),
> 禁直提 main；写文件用 shell heredoc；代码注释/文档不点名任何参考项目。

## Codex-A — `feat/2026-08-24-cloud-deps`（第三次催办,最高优先）

云依赖问题已确认两处:boto3 缺失致裸环境 11 个文件收集错误、kubernetes SDK 缺失
致 test_k8s_rbac 3 failed + 12 errors。
任务：pyproject 声明 `[project.optional-dependencies] cloud` extras,云模块
boto3/kubernetes 等改懒加载;裸环境 `pip install -e .` 后 `pytest` 收集零错误;
补一个模拟裸环境(隐藏已装包)的收集测试。完成后 main 全量测试应回到
**964+ passed** 口径(k8s_rbac 一并修复)。

## Claude 主力 — `feat/2026-08-24-scope-retest`

ScopeGuardHook 误拦已修(08-22 合入),但未在真实靶场复测。
任务：对自建靶场 `http://103.236.66.228:3333/` 重跑非 dry-run smoke,对比
08-19 基线:deny 次数应从 48 次误拦降到≈0、recon 覆盖面是否因此扩大、
总耗时变化。产出 `docs/release/smoke-2026-08-24.md` 对比表。

## Codex-B — `feat/2026-08-24-release-v020`

编排器已成形(Manager 聚合/验证池/语义记忆/自愈/smoke 实测),功能体量够发版。
任务：整理 `CHANGELOG.md` 提炼 v0.2.0 release notes(中英),打 tag `v0.2.0`
并创建 GitHub Release(gh CLI,署名 cdxiaodong);附 bench 编排对比数据摘要。
发版是外发动作,tag 内容先在分支内准备好,**打 tag 前等监工确认**。

---

交付标准：测试全绿 + 署名正确 + 各自分支 push origin,完成后 mailbox 汇报。
