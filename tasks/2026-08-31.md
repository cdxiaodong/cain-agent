# 2026-08-31 派活单（Lead · 夜间抢救）

> 派活：2026-08-31 20:10（09:30/14:00 窗口因会话切换中断，Lead 失职补执行）
> 背景：工程师会话仍停摆（未恢复）；今日产线至 20:05 零活动；b9 会话已不可见。
> 监工临时会话 20:00 前后活跃（claude-temp-2d540c35 / claude-temp-1c53cc26）。
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；独立分支/worktree，禁直提 main。

## 任务（夜班，执行者优先级从上到下）

### 1. `test/2026-08-29-vuln-tf-bench`（顺延第三日，最高优先，Phase 2 最后闭环项）

worktree 已在 /Users/cdxd/Desktop/develop/cain-agent-vuln-tf-bench（基线 ce3eef0，
开工先平移到最新 main：`git fetch && git reset --hard origin/main`，worktree 干净零改动）。

任务：BenchmarkExecutor 对 vuln-tf 三场景（OSS 公开桶 / RAM 过度授权 / RAM 管理员）
离线静态跑分：检出数 / 误报数（对照桶应零命中）/ 耗时 / token，报告写
`docs/release/bench-vulntf-2026-08-31.md`。不 apply、不触网、零真实凭证。

### 2. 若有余力：`feat/2026-08-31-ci-workflow`（可选）

GitHub Actions CI 工作流（.github/workflows/ci.yml）：push 触发
ruff + pytest（pip install -e .[dev,cloud]），零网络测试天然适配。
仅当任务 1 完成后接做。

---

交付标准：测试全绿 + 署名正确 + push origin + done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
