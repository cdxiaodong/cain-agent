# 2026-09-20 派活单

今天两任务推进 DESIGN §3.3 校验闭环收口（Phase 5-A 收官）与 BAC 链深化（Phase 5-D 前置）：A3 confirm 副作用门 + 5-A 组合回归。全离线纯函数切片。

## 基线与约束

- 基线 main=`6113e5d`（1223 passed / 3 skipped，ruff/pyright 全绿）。
- 执行模式：监工领取制（领取→先复现后改→自合并→done 披露取舍→Lead 事后独立复核）。
- 通用：独立分支、禁直提 main、交付后工作树切回 main；署名 cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>（提交前验证）；fix/feat/test(scope): 描述；三门全绿零回归；done 写 tasks/done-2026-09-20-*.md。
- 红线不变。不发布版本、不动 issue/PR。

## 1. fix：A3 confirm 副作用门（Phase 5-A 收官项）

分支 `feat/2026-09-20-side-effect-gate`。范围：`src/cain_agent/validator.py`、`findings.py`、`pipeline.py`、`report_markdown.py` 及对应测试。

按 ROADMAP A3 定义：
- validator 增 `side_effect_evidence`（副作用证据字段——写操作类 finding 需附"无副作用"证据，如只读响应/回滚留痕说明；证据仍只哈希不落明文）
- **无副作用证据的 confirmed 降级新状态 `likely`**（第五状态，FindingResult 枚举扩展）——注意四态到五态是**破坏性枚举扩展**：检查所有 match/遍历/序列化路径，旧数据（无该字段）语义保持 confirmed 不动，仅新流程产生 likely
- 验证池表决取保守（likely 不计入 confirmed 多数，按 inconclusive 处理或单独降档——二选一在 done 中说明取舍）
- 聚合（manager）与 report 同步呈现 likely

## 2. test：5-A 组合回归（A1 门 × A2 证据 × A4 分歧交互）

分支 `test/2026-09-20-phase5a-integration`。范围：新增 `tests/test_phase5a_integration.py`（不改 src/；若发现运行缺陷仅记录定位，按 09-09 任务 3 先例）。

覆盖三件已合入件的组合语义：
- coverage gate blocked → test 降级 dry-run → findings 为空 → 报告含 caveats 且无重放清单（链式语义）
- finding 带 replay 证据包 + 验证池分歧 → model_dissent 与重放清单同现于报告且互不污染
- gate 缺失/损坏 + replay 字段旧数据（无该键）→ 全链兼容无异常
- ≥8 例

---

交付标准：三门全绿 + 署名 + push origin + done 汇报 + 工作树归位。push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
