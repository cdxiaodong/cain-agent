# 2026-08-23 派活单（Lead 深夜补位）

> 派活：2026-08-23 23:45（09:30 cron 因 Mac 睡眠积压至深夜触发）
> 执行：工程师明日 09:30 唤醒后开工（本单任务对 08-24 有效）
> 红线：禁 pentest-agent-mvp / 泄漏 Claude Code / 平安信息；署名 cdxiaodong
> <84082748+cdxiaodong@users.noreply.github.com>；各自分支，禁直提 main；
> Codex 写文件用 shell heredoc；注释/文档不点名参考项目。

## 价值线（今日三任务推进 DESIGN.md 哪条线）

① 全链路验证（Phase 2.5 收官：scopeguard 修复的端到端确认）② 云依赖 CI 可用性（运维线）③ 测试强化（scope 修复的边界钉死）

## Claude 主力 — `feat/2026-08-24-scope-fix-smoke`

scopeguard 误拦修复（99727ba）已合入但未做过**编排模式全链路 smoke 复测**。
任务：对自建靶场跑一轮非 dry-run 编排 smoke，对照 08-19 smoke 报告中
"48 次 deny" 的误拦场景，验证修复后：重定向体/grep 正则/本地路径不再拦，
真实网络目标仍强制 scope 判定；`$()` 保持默认拒绝（安全语义确认）。
产出 `docs/release/smoke-scope-fix-2026-08-24.md`（只记录不修）。

## Codex-A — `feat/2026-08-21-cloud-deps`（顺延第三日，今日必交付）

分支已建未提交。任务：pyproject 声明 cloud extras 或云模块懒加载，
保证 `pip install -e .` 裸环境 `pytest` 收集零错误（当前 11 个
ModuleNotFoundError + test_k8s_rbac 缺 kubernetes SDK）；补 CI 式裸环境
收集测试。这是 CI 全绿的前置阻塞项，优先级最高。

## Codex-B — `test/2026-08-24-scope-edge-tests`

为 99727ba 修复补**边界与异常路径测试**（职责：测试强化）：
- 极端命令形态：管道串联/多重重定向/嵌套引号/unicode 路径
- 提取器故障注入（畸型 curl 行）
- deny 语义反向钉死：真实越界目标（非白名单 host）必须仍被拦截
- 与 cli.py `_scope_entry` 的一致性对照
确认不放宽：新增用例中"真实网络目标越界"拒绝率必须 100%。

---

交付标准：测试全绿 + 署名正确 + 分支提交并 push origin，完成写 done 汇报。
push 备用（SSH 被拦时）：`git -c credential.helper='!gh auth git-credential' push https://github.com/cdxiaodong/cain-agent.git HEAD:<分支名>`
