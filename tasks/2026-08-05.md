# 产线派活 · 2026-08-05(Day 3)

> Lead:Claude · 当前阶段:**Phase 1 收尾(剩 Docker)+ Phase 2 校验闭环开工**
> 依据:`/Users/cdxd/Desktop/develop/cloudpentest-agent/DESIGN.md` + ROADMAP.md + CHANGELOG.md
> 昨日状态:Orchestrator/Workspace、OSS 暴露检测、Web 三技能全部合入,main 停在 `559bb6a`,115 测试全绿。
> 说明:今日派活因机器休眠延迟到午后触发,成员被唤醒后立即开工即可,验收顺延到 21:00。

## 全局强制要求(三人通用,违反一律打回)

1. **分支隔离**:各自在指定新分支工作(从最新 main 切出),**禁止直接提交 main**,禁止改别人的文件范围。
2. **commit 署名**:所有 commit 必须是 `cdxiaodong <84082748+cdxiaodong@users.noreply.github.com>`。提交前执行 `git config user.email` 验证,不对先修正再提交。
3. **commit message 规范**:`feat(scope): 描述` / `test(scope): 描述` / `docs(scope): 描述`,禁止无意义 message。
4. **质量门**:提交前本地 `ruff check src tests`、`pyright`、`pytest` 三绿。
5. **完成后汇报**:在仓库根写 `done-XX.md`(分支名、改动文件清单、自测结果、验收对照),Codex 写文件一律用 shell heredoc,**别用 apply_patch**。
6. **红线**:不使用 pentest-agent-mvp 任何代码;不引入泄漏版 Claude Code 代码;Agent 引擎只用 Claude Agent SDK;不涉及任何平安内部信息;云操作一律只读。

---

## 任务 1 ·【Claude 主力工程师】Finding 校验数据模型 + 指纹去重 + 定级规则表(Phase 2 开工)

- **分支**:`core/2026-08-05-finding-validator`
- **目标**:DESIGN.md §3.3 校验闭环的核心件。先把"发现"的数据结构、去重、定级规则这三件确定性工程做死,后续校验 Agent 分离(独立 session)只是执行层组装。
- **改动范围**(只允许动这些):
  - 新建 `src/cain_agent/findings.py`
  - 新建 `tests/test_findings.py`
  - **不改** `workspace.py` / `orchestrator.py` / `executor.py` / `scope.py`(已验收冻结,只 import)
- **功能要求**:
  1. `Finding` 数据类:字段对齐 DESIGN §3.3——`finding_id` / `result`(confirmed | false_positive | validation_system_error | validation_inconclusive 四状态枚举)/ `severity`(critical|high|medium|low|info)/ `evidence_hash`(sha256: 前缀,提供从证据文本算哈希的工具函数,**证据本身只哈希不落明文**)/ `reason`(≤30 字,超长拒绝)/ `cloud` / `service` / `resource` / `issue_type`。
  2. 指纹去重:`fingerprint(finding)` = cloud+service+resource+issue_type 四元组归一化(小写、去首尾空白)后的 sha256;`dedup(findings)` 保序去重(教训:CyberStrikeAI #178 重复漏洞淹没)。
  3. 定级规则表:`SeverityRule` 规则为**代码常量数据**(如 公开可写存储=critical、公开读=high、元数据端点可达=high、仅配置不规范=medium),`classify(finding) -> severity` 纯函数查表;模型只能给"建议 severity",最终定级必须过规则表,规则表无命中时取模型建议与 info 的较低者(教训:CyberStrikeAI #163 定级虚高)。
  4. `findings_json` 兼容:提供 `to_dict/from_dict`,能与 Workspace 的 findings.json 读写兼容(round-trip 无损)。
- **自测**:纯逻辑零触网。覆盖:四状态枚举非法值拒绝、reason 超长拒绝、四元组归一化去重(大小写/空白变体判同)、规则表优先级、无命中时降级取低、JSON round-trip。
- **不做**:不接校验 Agent 独立 session(执行层后续组装)、不动 OSS/Web 模块。

## 任务 2 ·【Codex-A】Docker 一键运行(Phase 1 收官件)

- **分支**:`feat/2026-08-05-docker`
- **目标**:ROADMAP Phase 1 最后一项「Docker 一键运行」。让任何人 `docker build` + `docker run` 就能跑起来,为演示视频打底。
- **改动范围**(只允许动这些):
  - 新建 `Dockerfile`、`.dockerignore`
  - 新建 `docs/docker.md`
  - 新建 `tests/test_docker_config.py`
- **功能要求**:
  1. `Dockerfile`:基础镜像 `python:3.11-slim`;多阶段或单阶段均可,但**非 root 用户运行**(新建 cain 用户);`pip install .` 安装项目;ENTRYPOINT 为 `cain-agent`,默认 CMD `--help`;构建时不拷贝 tests/docs/.git(配 .dockerignore)。
  2. 镜像内自检:构建后 `docker run --rm <image> --version` 能输出版本号;另提供 `docker run --rm <image> selftest` 不行就算了——不强求 CLI 加子命令,验证 --version 即可。
  3. `.dockerignore`:排除 .git/.venv/__pycache__/.pytest_cache/tests/docs/skills 之外必需项自行权衡,目标是镜像尽量小。
  4. `docs/docker.md`:构建/运行命令、镜像体积、如何在容器里挂 workspace 卷(`-v $(pwd)/workspace:/workspaces/...`)、阿里云凭证通过 `-e` 环境变量传入(**严禁**写进镜像)、只读原则声明。
  5. `tests/test_docker_config.py`:静态校验 Dockerfile 存在、含非 root USER 指令、ENTRYPOINT 指向 cain-agent、.dockerignore 排除 .git/.venv(不真的 build,CI 友好)。
  6. 本机实际 `docker build` 一次验证可用,把镜像体积和构建耗时写进汇报;若本机 Docker 不可用,在汇报中说明并保证静态测试全绿。
- **不做**:不做 docker-compose 编排(靶场 compose 是任务 3 的)、不录演示视频(需要人工,Lead 另行安排)、不动 CLI 代码。

## 任务 3 ·【Codex-B】本地回归靶场 compose + Benchmark 评测方案文档

- **分支**:`test/2026-08-05-bench-range`
- **目标**:DESIGN.md §3.5 评测体系的第一层(靶场回归,工程底线)落地配置 + 方案文档,为 Web 三技能提供可重复验证的靶标。
- **改动范围**(只允许动这些):
  - 新建 `bench/docker-compose.yml`
  - 新建 `docs/benchmark-plan.md`
  - 新建 `tests/test_bench_config.py`
- **功能要求**:
  1. `bench/docker-compose.yml`:两个开源靶标——OWASP Juice Shop(SQLi/XSS 面)和 DVWA(经典漏洞面),只绑定 `127.0.0.1` 端口(严禁暴露到局域网),注明仅用于本地授权回归测试;healthcheck 配上;不要 build 自定义镜像,直接用官方镜像。
  2. `docs/benchmark-plan.md`:两层评测体系方案——第一层靶场回归(检出率/误报率/耗时/token 成本四指标,XBOW 靶场介绍 + 本地 compose 靶场用法),第二层实战验证(仅合法授权渠道:漏洞盒子/补天/VDP、自有资产;授权证据留存要求);指标口径定义清楚(什么算检出、什么算误报)。
  3. `tests/test_bench_config.py`:静态校验 compose 文件 YAML 合法、两个服务端口绑定均含 `127.0.0.1`、无 privileged: true、无新增宿主敏感目录挂载(不起容器,CI 友好)。
  4. 若本机 Docker 可用,`docker compose -f bench/docker-compose.yml config` 验证语法,结果写汇报;不可用则说明。
- **不做**:不写实际测试执行脚本(引擎跑靶场是后续任务)、不动 skills/ 和 src/、不启动真实扫描。

---

## 验收标准(21:00 Lead 验收对照表)

| 任务 | 合并前提 |
|---|---|
| 1 Finding 校验 | 三绿;去重归一化/定级降级/round-trip 有测试钉死;冻结文件零改动 |
| 2 Docker | 三绿;静态测试钉死非 root/ENTRYPOINT/.dockerignore;本机 build 结果(或不可用说明)在汇报中 |
| 3 靶场配置 | 三绿;端口绑 127.0.0.1 有测试钉死;benchmark 文档两指标层口径清晰 |
