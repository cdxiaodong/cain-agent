# v0.2.0 Release Checklist(已归档)

> **状态:已发布** — tag `v0.2.0`(4b2026cf)与 GitHub Release 均于
> 2026-08-25 16:35 UTC 上线。本文归档为发版过程记录;
> 后续发版终备见 `v0.2.1-release-plan.md`(09-04 终检产物)。

## Prepared

- Package version: `0.2.0`
- Bilingual release notes: `docs/release/v0.2.0.md`
- Benchmark summary: classic versus orchestrated mode
- Version consistency test: `tests/test_release_v020.py`

## 事后核验(2026-09-04)

- tag 指向 c1324d2(v0.2.0 发版准备 merge),在 main 历史内,本地与远端一致
- GitHub Release `v0.2.0` 非 draft,published 2026-08-25T16:35:22Z

## Verification(发版时记录)

```text
pytest -q --ignore=tests/test_k8s_rbac.py
947 passed, 3 skipped

ruff check tests/test_release_v020.py
All checks passed

pyright src/cain_agent/__init__.py tests/test_release_v020.py
0 errors, 0 warnings, 0 informations
```

At preparation time, the unmodified K8s RBAC suite still fails in a bare environment
because `kubernetes` is not declared. The parallel cloud-dependency task must merge
before the release tag is created.

## Release Gates(均已满足)

1. ~~Merge the cloud-dependency fix and verify the full suite on updated `main`.~~
   已满足:K8s RBAC 套件已并入默认全量套件。
2. ~~Obtain release-manager confirmation for the external release.~~ 已确认并发布。
3. ~~Confirm the annotated tag does not already exist.~~ 发版时未占用。

## Commands After Confirmation

Run from updated `main`:

```bash
git tag -a v0.2.0 -m "cain-agent v0.2.0: orchestration and verification release"
git push origin v0.2.0
gh release create v0.2.0 \
  --title "cain-agent v0.2.0" \
  --notes-file docs/release/v0.2.0.md \
  --verify-tag
```
